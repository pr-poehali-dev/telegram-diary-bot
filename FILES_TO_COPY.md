# 📂 Полный список файлов для копирования

## 🎯 Структура переноса

Копируем из **Ежедневника** → в **access-bars-service**

---

## 📁 Frontend файлы (src/)

### Основные компоненты (/src/components/):
```
ScheduleTab.tsx           → Главный компонент календаря с записями
ScheduleCycleManager.tsx  → Менеджер циклов учёбы (2-недельный)
DashboardTab.tsx          → Дашборд со статистикой
Sidebar.tsx               → Боковое меню (если нужно)
TabsContent.tsx           → Контент вкладок
```

### Компоненты расписания (/src/components/webapp/schedule/):
```
WeekScheduleCard.tsx      → Карточка недельного расписания
EventsCard.tsx            → Карточка мероприятий
ConflictDialog.tsx        → Диалог конфликтов
BlockedDatesCard.tsx      → Карточка заблокированных дат
```

### Компоненты бронирования (/src/components/booking/):
```
BookingStepIndicator.tsx  → Индикатор шагов бронирования
ClientDataStep.tsx        → Шаг ввода данных клиента
DateTimeSelectionSteps.tsx → Шаг выбора даты/времени
ServiceSelectionStep.tsx  → Шаг выбора услуги
TelegramNotificationDialog.tsx → Диалог уведомлений Telegram
```

### UI компоненты (/src/components/ui/):
**ВАЖНО**: Если у вас уже есть shadcn/ui компоненты, пропустите этот раздел!
Нужны только если отсутствуют:
- calendar.tsx (компонент календаря)
- badge.tsx, button.tsx, card.tsx, alert.tsx и т.д.

---

## 🧩 Контексты (/src/contexts/):

```
AppContext.tsx   → Контекст приложения (управление UI)
AuthContext.tsx  → Контекст аутентификации
DataContext.tsx  → ⚠️ ГЛАВНЫЙ - управление данными (bookings, events, schedule)
```

**⚠️ КРИТИЧЕСКИ ВАЖНО**: `DataContext.tsx` - это центр управления всеми данными ежедневника!

---

## 🪝 Хуки (/src/hooks/):

```
useApi.ts        → Хук для работы с API
use-toast.ts     → Хук для уведомлений (toast)
```

---

## 🔧 Утилиты и сервисы:

```
/src/services/api.ts  → API клиент
/src/utils/cache.ts   → Кэширование данных
/src/lib/utils.ts     → Вспомогательные утилиты
```

---

## 🖥️ Backend функции (/backend/):

### Основные функции API (/backend/api/):
```
index.py          → Главный API endpoint
tests.json        → Тесты для API
requirements.txt  → Зависимости Python
```

### Аутентификация (/backend/auth/):
```
index.ts          → TypeScript функция авторизации
tests.json
package.json
```

### Админ-панель (/backend/admin-auth/):
```
index.py
tests.json
requirements.txt
```

### Telegram бот (если нужен) (/backend/telegram-bot/):
```
index.py          → Обработчик Telegram запросов
tests.json
requirements.txt
```

---

## 💾 База данных:

### SQL миграции (готовые файлы):
```
MIGRATION_CREATE_TABLES.sql  → Создание всех таблиц
MIGRATION_INITIAL_DATA.sql   → Начальные данные
```

---

## ⚙️ Интеграция в access-bars-service

### 1. Обновить App.tsx:
Добавить роуты:
```typescript
<Route path="/admin/schedule" element={
  <ProtectedRoute>
    <AuthProvider>
      <AppProvider>
        <DataProvider>
          <AdminPanel />
        </DataProvider>
      </AppProvider>
    </AuthProvider>
  </ProtectedRoute>
} />
```

### 2. Обновить BookingForm.tsx:
Интегрировать с DataContext:
```typescript
import { useData } from '@/contexts/DataContext';

const { refreshBookings, bookings } = useData();

// После успешной записи:
await refreshBookings();
```

### 3. Обновить Navigation / Menu:
Добавить ссылку на календарь:
```typescript
<Link to="/admin/schedule">Календарь</Link>
```

---

## ✅ Порядок действий

### В проекте access-bars-service:

1. **Применить SQL миграции**:
   - Выполнить `MIGRATION_CREATE_TABLES.sql`
   - Выполнить `MIGRATION_INITIAL_DATA.sql` (с правками owner_id)

2. **Скопировать контексты** (самое важное):
   - `src/contexts/AppContext.tsx`
   - `src/contexts/AuthContext.tsx`
   - `src/contexts/DataContext.tsx` ⚠️

3. **Скопировать хуки и утилиты**:
   - `src/hooks/useApi.ts`
   - `src/services/api.ts`
   - `src/utils/cache.ts`

4. **Скопировать компоненты**:
   - Основные: `ScheduleTab.tsx`, `ScheduleCycleManager.tsx`
   - Подкомпоненты: всё из `/schedule`, `/booking`

5. **Скопировать backend функции**:
   - Папки: `/backend/api`, `/backend/auth`, `/backend/admin-auth`

6. **Запустить sync_backend** в access-bars-service

7. **Обновить интеграцию**:
   - Добавить роуты в `App.tsx`
   - Обновить `BookingForm.tsx`
   - Добавить навигацию

---

## 🎯 Результат

После переноса в access-bars-service:
- ✅ Полнофункциональный календарь на `/admin/schedule`
- ✅ Все записи из `BookingForm` попадают в ежедневник
- ✅ Управление: мероприятия, блокировки, расписание учёбы
- ✅ Дашборд со статистикой
- ✅ Обнаружение конфликтов

---

## 📞 Готово к переносу!

Всё подготовлено для переноса в первый проект. 
Ссылка на GitHub этого проекта понадобится для автоматического копирования файлов.
